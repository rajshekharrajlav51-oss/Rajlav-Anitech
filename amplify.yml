version: 1
applications:
  - appRoot: artifacts/rajlav-animation
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm
            - pnpm install --no-frozen-lockfile   # ✅ FIX
        build:
          commands:
            - pnpm build
      artifacts:
        baseDirectory: dist/public
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*