{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "corepack pnpm --dir artifacts/rajlav-animation dev",
    "dev:frontend": "corepack pnpm --dir artifacts/rajlav-animation dev",
    "dev:api": "corepack pnpm --dir artifacts/api-server dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "pnpm": "^9.0.0"
  }
}
